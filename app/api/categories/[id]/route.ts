import {getDb} from "@/db";import {categories} from "@/db/schema";import {eq} from "drizzle-orm";import {requireAdmin} from "@/lib/auth";
export async function PATCH(req:Request,{params}:{params:Promise<{id:string}>}){if(!await requireAdmin())return Response.json({error:"غير مخول"},{status:401});const x=await req.json();await getDb().update(categories).set({name:x.name,icon:x.icon||"🧸",imageUrl:x.imageUrl||null,displayShape:x.displayShape||"rounded",displayMode:x.displayMode||"icon",parentId:x.parentId?+x.parentId:null,sortOrder:+x.sortOrder||0,active:x.active!==false}).where(eq(categories.id,+(await params).id));return Response.json({ok:true})}
export async function DELETE(_:Request,{params}:{params:Promise<{id:string}>}){if(!await requireAdmin())return Response.json({error:"غير مخول"},{status:401});await getDb().delete(categories).where(eq(categories.id,+(await params).id));return Response.json({ok:true})}
