import {verifyLogin} from "@/lib/auth";import {cookies} from "next/headers";export async function POST(req:Request){try{const {username,password}=await req.json();const token=await verifyLogin(String(username||""),String(password||""));if(!token)return Response.json({error:"اسم المستخدم أو كلمة المرور غير صحيحة"},{status:401});(await cookies()).set("admin_session",token,{httpOnly:true,secure:true,sameSite:"lax",path:"/",maxAge:2592000});return Response.json({ok:true})}catch(error){console.error("Admin login failed",error);return Response.json({error:"حدث خطأ مؤقت أثناء تسجيل الدخول، حاول مرة أخرى"},{status:500})}}
