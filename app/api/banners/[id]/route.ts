import {getDb} from "@/db";import {banners} from "@/db/schema";import {eq} from "drizzle-orm";import {requireAdmin} from "@/lib/auth";
export async function PATCH(req:Request,{params}:{params:Promise<{id:string}>}){if(!await requireAdmin())return Response.json({error:"غير مخول"},{status:401});const id=+(await params).id,x=await req.json();await getDb().update(banners).set({title:x.title,subtitle:x.subtitle||null,imageUrl:x.imageUrl||null,buttonText:x.buttonText||null,buttonLink:x.buttonLink||null,style:x.style||null,sortOrder:+x.sortOrder||0,active:x.active!==false}).where(eq(banners.id,id));return Response.json({ok:true})}
export async function DELETE(_:Request,{params}:{params:Promise<{id:string}>}){if(!await requireAdmin())return Response.json({error:"غير مخول"},{status:401});await getDb().delete(banners).where(eq(banners.id,+(await params).id));return Response.json({ok:true})}
